# Fase 2 — Slice 1+2 (Backend Vercel + Capacitor iOS) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mettere l'app sull'iPhone dell'utente come app Capacitor che mostra eventi reali presi da un'API Next.js deployata su Vercel (approccio B: UI statica nel bundle + API remota).

**Architecture:** Il backend `apps/web` (UI+API) resta su Vercel con i segreti server-side. Per l'app mobile si esporta la sola UI statica (`output: 'export'`) che chiama `${NEXT_PUBLIC_API_BASE}/api/events` su Vercel; Capacitor la impacchetta in un'app iOS/Android. La posizione usa il plugin nativo Geolocation.

**Tech Stack:** Next.js 15 (App Router), Capacitor 6, @capacitor/geolocation, Vercel, Vitest, TypeScript strict.

---

## File Structure

- **Create** `apps/web/lib/api.ts` — `apiBase()` + `eventsUrl(params)` (client usa base configurabile).
- **Create** `apps/web/test/api.test.ts` — test del builder URL.
- **Create** `apps/web/lib/cors.ts` — `corsHeaders()` per la route API.
- **Create** `apps/web/test/cors.test.ts` — test header CORS.
- **Modify** `apps/web/app/api/events/route.ts` — CORS + handler `OPTIONS` + guardia `CAPACITOR_BUILD` + `dynamic` condizionale.
- **Create** `apps/web/lib/geo.ts` — `chooseGeoProvider(isNative)` + `getCurrentPosition()` (Capacitor vs web).
- **Create** `apps/web/test/geo.test.ts` — test selezione provider.
- **Modify** `apps/web/app/page.tsx` — usa `eventsUrl()` + `getCurrentPosition()`.
- **Modify** `apps/web/next.config.mjs` — export statico quando `CAPACITOR_BUILD=1`.
- **Create** `apps/web/capacitor.config.ts` — config Capacitor (appId/appName/webDir).
- **Modify** `apps/web/package.json` — dipendenze Capacitor + script build:cap / cap:sync / cap:ios.
- **Modify** `vitest.config.ts` — includere i test di `apps/web`.
- **Modify** `.env.example` — `NEXT_PUBLIC_API_BASE`.

---

# SLICE 1 — Backend su Vercel + API base + CORS

## Task 1: Client API base configurabile

**Files:**
- Create: `apps/web/lib/api.ts`
- Create: `apps/web/test/api.test.ts`
- Modify: `vitest.config.ts`

- [ ] **Step 1: Estendere vitest include**

Modify `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: [
      "packages/**/test/**/*.test.ts",
      "config/test/**/*.test.ts",
      "apps/web/test/**/*.test.ts",
    ],
    environment: "node",
  },
});
```

- [ ] **Step 2: Write the failing test**

Create `apps/web/test/api.test.ts`:

```ts
import { afterEach, describe, expect, it } from "vitest";
import { apiBase, eventsUrl } from "../lib/api";

afterEach(() => {
  delete process.env.NEXT_PUBLIC_API_BASE;
});

describe("apiBase", () => {
  it("vuoto di default (stesso origin sul web)", () => {
    expect(apiBase()).toBe("");
  });
  it("usa NEXT_PUBLIC_API_BASE e toglie lo slash finale", () => {
    process.env.NEXT_PUBLIC_API_BASE = "https://eventi.vercel.app/";
    expect(apiBase()).toBe("https://eventi.vercel.app");
  });
});

describe("eventsUrl", () => {
  it("costruisce il path relativo di default", () => {
    expect(eventsUrl({ lat: 41.9, lng: 12.5, city: "Roma" })).toBe(
      "/api/events?lat=41.9&lng=12.5&city=Roma",
    );
  });
  it("antepone la base remota quando presente", () => {
    process.env.NEXT_PUBLIC_API_BASE = "https://eventi.vercel.app";
    expect(eventsUrl({ lat: 41.9, lng: 12.5 })).toBe(
      "https://eventi.vercel.app/api/events?lat=41.9&lng=12.5",
    );
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `node node_modules/vitest/vitest.mjs run api`
Expected: FAIL — cannot find module `../lib/api`.

- [ ] **Step 4: Implement `apps/web/lib/api.ts`**

```ts
/**
 * Base URL dell'API. Vuoto sul web (stesso origin); nell'app Capacitor punta
 * al deploy Vercel via NEXT_PUBLIC_API_BASE (inlined a build time da Next).
 */
export function apiBase(): string {
  return (process.env.NEXT_PUBLIC_API_BASE ?? "").replace(/\/$/, "");
}

export type EventsParams = { lat: number; lng: number; city?: string };

/** URL completo per GET /api/events con i parametri dati. */
export function eventsUrl(p: EventsParams): string {
  const qs = new URLSearchParams({ lat: String(p.lat), lng: String(p.lng) });
  if (p.city) qs.set("city", p.city);
  return `${apiBase()}/api/events?${qs.toString()}`;
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `node node_modules/vitest/vitest.mjs run api`
Expected: PASS (4 tests).

- [ ] **Step 6: Commit**

```bash
git add apps/web/lib/api.ts apps/web/test/api.test.ts vitest.config.ts
git commit -m "feat(web): configurable API base URL for mobile client"
```

---

## Task 2: CORS + OPTIONS + guardia export sulla route

**Files:**
- Create: `apps/web/lib/cors.ts`
- Create: `apps/web/test/cors.test.ts`
- Modify: `apps/web/app/api/events/route.ts`

- [ ] **Step 1: Write the failing test**

Create `apps/web/test/cors.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { corsHeaders } from "../lib/cors";

describe("corsHeaders", () => {
  it("consente qualsiasi origin (app Capacitor) e i metodi GET/OPTIONS", () => {
    const h = corsHeaders();
    expect(h["Access-Control-Allow-Origin"]).toBe("*");
    expect(h["Access-Control-Allow-Methods"]).toContain("GET");
    expect(h["Access-Control-Allow-Methods"]).toContain("OPTIONS");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node node_modules/vitest/vitest.mjs run cors`
Expected: FAIL — cannot find module `../lib/cors`.

- [ ] **Step 3: Implement `apps/web/lib/cors.ts`**

```ts
/**
 * Header CORS per /api/events. L'app Capacitor chiama da origin
 * `capacitor://localhost` (iOS) / `http://localhost` (android): consentiamo *.
 * La route e' di sola lettura (GET) e non usa cookie, quindi `*` e' sicuro.
 */
export function corsHeaders(): Record<string, string> {
  return {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node node_modules/vitest/vitest.mjs run cors`
Expected: PASS (1 test).

- [ ] **Step 5: Update the route to send CORS, handle OPTIONS, and support static export**

Replace `apps/web/app/api/events/route.ts` with:

```ts
import { runPipeline } from "@eventi/sources";
import { NextResponse } from "next/server";
import { buildGeoQuery, isValidLatLng } from "@/lib/query";
import { corsHeaders } from "@/lib/cors";

// In build Capacitor (export statico) la route viene prerenderizzata vuota:
// l'app usa l'API remota su Vercel, non quella nel bundle.
const isCapacitorBuild = process.env.CAPACITOR_BUILD === "1";

export const runtime = "nodejs";
export const dynamic = isCapacitorBuild ? "force-static" : "force-dynamic";

const EMPTY = {
  query: null,
  meta: { rawCount: 0, dedupedCount: 0, finalCount: 0, sources: [], failed: [], usedMock: false },
  events: [],
};

/** Preflight CORS per l'app mobile. */
export function OPTIONS(): NextResponse {
  return new NextResponse(null, { status: 204, headers: corsHeaders() });
}

/**
 * GET /api/events?lat=&lng=&radius=&from=&to=&city=
 * Ritorna Event[] gia' rankati e filtrati (filtro adattivo applicato).
 */
export async function GET(req: Request): Promise<NextResponse> {
  if (isCapacitorBuild) {
    return NextResponse.json(EMPTY, { headers: corsHeaders() });
  }

  const { searchParams } = new URL(req.url);
  const query = buildGeoQuery(searchParams);

  if (!isValidLatLng(query)) {
    return NextResponse.json(
      { error: "Parametri lat/lng mancanti o non validi" },
      { status: 400, headers: corsHeaders() },
    );
  }

  try {
    const result = await runPipeline(query);
    return NextResponse.json(
      {
        query,
        meta: {
          rawCount: result.rawCount,
          dedupedCount: result.dedupedCount,
          finalCount: result.events.length,
          sources: result.sources,
          failed: result.failed,
          usedMock: result.usedMock,
        },
        events: result.events,
      },
      { headers: corsHeaders() },
    );
  } catch (err) {
    return NextResponse.json(
      { error: "Errore pipeline", detail: (err as Error).message },
      { status: 500, headers: corsHeaders() },
    );
  }
}
```

- [ ] **Step 6: Verify build + typecheck**

Run: `pnpm --filter @eventi/web build`
Expected: build OK (route dynamic, normale).

Run: `pnpm typecheck`
Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add apps/web/lib/cors.ts apps/web/test/cors.test.ts apps/web/app/api/events/route.ts
git commit -m "feat(web): CORS + OPTIONS on /api/events, static-export guard"
```

---

## Task 3: Deploy backend su Vercel (manuale — richiede tuo account)

**Files:** nessuno (deploy + env). Operazione una tantum sul tuo account Vercel.

> Questa task NON è automatizzabile in test: serve il tuo login Vercel.

- [ ] **Step 1: Login Vercel**

Nella sessione, esegui tu (prefisso `!`):
```
! npx vercel@latest login
```
Segui il browser per autenticarti.

- [ ] **Step 2: Link del progetto**

Run dalla root:
```bash
npx vercel@latest link --yes
```
Quando chiede la directory del progetto, indica `apps/web`. (Se chiede "In which scope", scegli il tuo account.)

- [ ] **Step 3: Imposta le env var su Vercel (segrete, server-side)**

```bash
cd apps/web
printf '%s' "$(grep '^SERPAPI_KEY=' ../../.env | cut -d= -f2)" | npx vercel@latest env add SERPAPI_KEY production
printf '%s' "serpapi" | npx vercel@latest env add EVENT_SOURCE_IT production
cd ../..
```
(Se hai TICKETMASTER_KEY / SPOTIFY_* aggiungile allo stesso modo. NON impostare NEXT_PUBLIC_API_BASE qui: il sito web usa lo stesso origin.)

- [ ] **Step 4: Deploy in produzione**

```bash
cd apps/web && npx vercel@latest --prod --yes && cd ../..
```
Annota l'URL prod restituito (es. `https://app-eventi-xxx.vercel.app`).

- [ ] **Step 5: Verifica API live**

```bash
curl -s "https://<TUO-URL>.vercel.app/api/events?lat=41.9028&lng=12.4964&city=Roma" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d);console.log('eventi:',j.events?.length,'fonti:',j.meta?.sources)})"
```
Expected: `eventi: >0  fonti: [ 'google_events_serpapi', 'resident_advisor' ]` (no `mock`).

- [ ] **Step 6: Verifica preflight CORS**

```bash
curl -s -i -X OPTIONS "https://<TUO-URL>.vercel.app/api/events" | grep -i "access-control-allow-origin"
```
Expected: `access-control-allow-origin: *`.

Nessun commit (deploy esterno). Annota l'URL prod: serve allo Slice 2.

---

# SLICE 2 — App Capacitor su iPhone

## Task 4: Geolocation provider (nativo vs web)

**Files:**
- Create: `apps/web/lib/geo.ts`
- Create: `apps/web/test/geo.test.ts`
- Modify: `apps/web/package.json` (deps Capacitor runtime)

- [ ] **Step 1: Installa i pacchetti Capacitor runtime (servono al typecheck di geo.ts)**

```bash
pnpm --filter @eventi/web add @capacitor/core @capacitor/geolocation
```

- [ ] **Step 2: Write the failing test**

Create `apps/web/test/geo.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { chooseGeoProvider } from "../lib/geo";

describe("chooseGeoProvider", () => {
  it("usa Capacitor su piattaforma nativa", () => {
    expect(chooseGeoProvider(true)).toBe("capacitor");
  });
  it("usa il browser sul web", () => {
    expect(chooseGeoProvider(false)).toBe("web");
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `node node_modules/vitest/vitest.mjs run geo`
Expected: FAIL — cannot find module `../lib/geo`.

- [ ] **Step 4: Implement `apps/web/lib/geo.ts`**

```ts
/**
 * Posizione: nativa via Capacitor Geolocation nell'app, navigator.geolocation
 * sul web. La scelta e' isolata in chooseGeoProvider per essere testabile.
 */
export type GeoProvider = "capacitor" | "web";

export function chooseGeoProvider(isNative: boolean): GeoProvider {
  return isNative ? "capacitor" : "web";
}

export type Coords = { lat: number; lng: number };

/** Ottiene la posizione corrente usando il provider giusto per la piattaforma. */
export async function getCurrentPosition(): Promise<Coords> {
  const { Capacitor } = await import("@capacitor/core");
  const provider = chooseGeoProvider(Capacitor.isNativePlatform());

  if (provider === "capacitor") {
    const { Geolocation } = await import("@capacitor/geolocation");
    const pos = await Geolocation.getCurrentPosition();
    return { lat: pos.coords.latitude, lng: pos.coords.longitude };
  }

  return new Promise<Coords>((resolve, reject) => {
    if (!navigator.geolocation) {
      reject(new Error("Geolocalizzazione non disponibile"));
      return;
    }
    navigator.geolocation.getCurrentPosition(
      (p) => resolve({ lat: p.coords.latitude, lng: p.coords.longitude }),
      (e) => reject(new Error(e.message)),
    );
  });
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `node node_modules/vitest/vitest.mjs run geo`
Expected: PASS (2 tests).

- [ ] **Step 6: Commit**

```bash
git add apps/web/lib/geo.ts apps/web/test/geo.test.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat(web): geolocation provider abstraction (capacitor/web)"
```

---

## Task 5: Pagina usa API base + geolocation nativa

**Files:**
- Modify: `apps/web/app/page.tsx`

- [ ] **Step 1: Aggiorna gli import in `apps/web/app/page.tsx`**

Sotto gli import esistenti dei componenti, aggiungi:

```ts
import { eventsUrl } from "@/lib/api";
import { getCurrentPosition } from "@/lib/geo";
```

- [ ] **Step 2: Sostituisci la fetch per usare `eventsUrl`**

Nella funzione `load`, sostituisci il blocco che costruisce `params` e chiama `fetch`:

```ts
      const res = await fetch(eventsUrl({ lat: l.lat, lng: l.lng, ...(l.city ? { city: l.city } : {}) }));
```

(Rimuovi le righe che costruivano manualmente `URLSearchParams` e l'URL `/api/events?...`.)

- [ ] **Step 3: Sostituisci `useMyLocation` con il provider nativo**

Rimpiazza la funzione `useMyLocation` con:

```tsx
  const useMyLocation = async (): Promise<void> => {
    try {
      const { lat, lng } = await getCurrentPosition();
      setLoc({ lat, lng, label: "La mia posizione" });
    } catch (e) {
      setError((e as Error).message);
    }
  };
```

E nel bottone, gestisci la promise:

```tsx
        <button
          className="rounded-full border border-white/10 bg-surface px-4 py-2 text-sm hover:bg-surface-2"
          onClick={() => void useMyLocation()}
        >
          Usa la mia posizione
        </button>
```

- [ ] **Step 4: Verify build + typecheck**

Run: `pnpm --filter @eventi/web build`
Expected: build OK.

Run: `pnpm typecheck`
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/page.tsx
git commit -m "feat(web): page uses remote API base + native geolocation"
```

---

## Task 6: Dipendenze Capacitor + export statico + config

**Files:**
- Modify: `apps/web/package.json`
- Modify: `apps/web/next.config.mjs`
- Create: `apps/web/capacitor.config.ts`

- [ ] **Step 1: Installa CLI + piattaforme Capacitor**

(`@capacitor/core` e `@capacitor/geolocation` sono già stati installati nella Task 4.)

```bash
pnpm --filter @eventi/web add -D @capacitor/cli
pnpm --filter @eventi/web add @capacitor/ios @capacitor/android
```

- [ ] **Step 2: Aggiungi gli script in `apps/web/package.json`**

Nel blocco `"scripts"` aggiungi:

```json
    "build:cap": "CAPACITOR_BUILD=1 NEXT_PUBLIC_API_BASE=$NEXT_PUBLIC_API_BASE next build",
    "cap:sync": "cap sync",
    "cap:ios": "cap run ios",
    "cap:open:ios": "cap open ios"
```

- [ ] **Step 3: Export statico condizionale in `apps/web/next.config.mjs`**

Replace the file with:

```js
/** @type {import('next').NextConfig} */
const isCapacitor = process.env.CAPACITOR_BUILD === "1";

const nextConfig = {
  reactStrictMode: true,
  // build per Capacitor: export statico in ./out (webDir dell'app)
  ...(isCapacitor ? { output: "export", images: { unoptimized: true } } : {}),
  // il lint del monorepo gira con `pnpm lint` (flat config alla root)
  eslint: { ignoreDuringBuilds: true },
  transpilePackages: ["@eventi/core", "@eventi/config", "@eventi/sources"],
  serverExternalPackages: ["playwright", "playwright-core", "chromium-bidi"],
};

export default nextConfig;
```

- [ ] **Step 4: Crea `apps/web/capacitor.config.ts`**

```ts
import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.marazzi.eventi",
  appName: "Eventi",
  // output statico di `next build` con CAPACITOR_BUILD=1
  webDir: "out",
};

export default config;
```

- [ ] **Step 5: Genera l'export statico (verifica che funzioni)**

```bash
cd apps/web && NEXT_PUBLIC_API_BASE="https://<TUO-URL>.vercel.app" pnpm build:cap && ls out/index.html && cd ../..
```
Expected: `out/index.html` esiste (export riuscito; nessun errore sulla route API grazie alla guardia).

- [ ] **Step 6: Commit**

```bash
git add apps/web/package.json apps/web/next.config.mjs apps/web/capacitor.config.ts pnpm-lock.yaml
git commit -m "feat(mobile): Capacitor deps + static export build + config"
```

---

## Task 7: Setup toolchain iOS (manuale, una tantum)

**Files:** nessuno (setup macchina). Richiede il tuo Mac + Apple ID.

- [ ] **Step 1: Punta xcode-select a Xcode.app**

Esegui tu (serve sudo):
```
! sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
```
Verifica:
```bash
xcodebuild -version
```
Expected: stampa la versione di Xcode (non l'errore CommandLineTools).

- [ ] **Step 2: Installa CocoaPods pulito (quello di sistema è rotto)**

```bash
brew install cocoapods
pod --version
```
Expected: stampa una versione (es. `1.15.x`) senza i `LoadError` di ffi.

- [ ] **Step 3: Accetta licenza e first launch Xcode**

```
! sudo xcodebuild -license accept
```
Apri Xcode una volta (installa i componenti se richiesto).

- [ ] **Step 4: Login Apple ID in Xcode**

In Xcode → Settings → Accounts → "+" → Apple ID (gratuito va bene). Questo crea un "Personal Team" per la firma.

Nessun commit.

---

## Task 8: Aggiungi iOS, sincronizza e gira sull'iPhone (manuale)

**Files:** Create `apps/web/ios/**`, `apps/web/android/**` (generati da Capacitor). Vanno committati.

- [ ] **Step 1: Inizializza le piattaforme native**

```bash
cd apps/web
NEXT_PUBLIC_API_BASE="https://<TUO-URL>.vercel.app" pnpm build:cap
npx cap add ios
npx cap add android
npx cap sync
cd ../..
```
Expected: cartelle `apps/web/ios` e `apps/web/android` create; `cap sync` copia `out/` nel progetto iOS.

- [ ] **Step 2: Permesso posizione iOS**

In `apps/web/ios/App/App/Info.plist` aggiungi dentro il `<dict>` principale:

```xml
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Serve la tua posizione per mostrarti gli eventi vicino a te.</string>
```

- [ ] **Step 3: Apri il progetto iOS in Xcode**

```bash
cd apps/web && npx cap open ios && cd ../..
```

- [ ] **Step 4: Firma con il tuo team**

In Xcode: target **App** → tab **Signing & Capabilities** → spunta "Automatically manage signing" → seleziona il tuo **Personal Team**. Se il bundle id è in conflitto, cambia `appId` in `capacitor.config.ts` (es. `com.marazzi.eventi2`) e ri-sync.

- [ ] **Step 5: Collega l'iPhone e fai girare**

- Collega l'iPhone via cavo, sbloccalo, "Trust This Computer".
- In Xcode seleziona il tuo device in alto, premi ▶ (Run).
- Sull'iPhone: Impostazioni → Generali → VPN e gestione dispositivi → fidati del tuo certificato sviluppatore.

Expected: l'app "Eventi" si apre sull'iPhone, mostra hero+feed con eventi reali (presi dall'API Vercel), e "Usa la mia posizione" chiede il permesso nativo.

- [ ] **Step 6: Commit dei progetti nativi**

```bash
git add apps/web/ios apps/web/android apps/web/capacitor.config.ts
git commit -m "feat(mobile): add iOS + Android Capacitor platforms"
```

> Promemoria: con Apple ID gratuito il certificato dura **7 giorni**. Per ri-testare dopo: ricollega e premi Run in Xcode (ri-firma).

---

## Task 9: Verifica finale + docs

**Files:**
- Modify: `.env.example`
- Modify: `README.md`

- [ ] **Step 1: Suite completa**

Run: `node node_modules/vitest/vitest.mjs run`
Expected: tutti verdi (core + config + sources + apps/web: api, cors, geo).

Run: `pnpm typecheck`
Expected: no errors.

Run: `node node_modules/eslint/bin/eslint.js .`
Expected: exit 0.

- [ ] **Step 2: Documenta env mobile in `.env.example`**

Aggiungi in fondo:

```
# --- App mobile (Capacitor) ---
# URL del backend Vercel che l'app chiama (NON usato dal sito web).
NEXT_PUBLIC_API_BASE=
```

- [ ] **Step 3: Sezione mobile in `README.md`**

Aggiungi una sezione "## App mobile (Capacitor)" con:
- come deployare il backend (Vercel, Task 3),
- `NEXT_PUBLIC_API_BASE=<url> pnpm --filter @eventi/web build:cap`,
- `npx cap sync && npx cap open ios`,
- nota certificato 7gg Apple ID gratuito,
- push iOS = futuro (Apple Developer a pagamento).

- [ ] **Step 4: Commit**

```bash
git add .env.example README.md
git commit -m "docs: mobile (Capacitor) build + deploy instructions"
```

---

## Note di implementazione

- **Slice 3 (Supabase) e 4 (push) sono piani separati**, non inclusi qui.
- **Segreti:** nel bundle dell'app finisce solo `NEXT_PUBLIC_API_BASE` (pubblico). `SERPAPI_KEY` resta solo su Vercel.
- **Scraper su Vercel:** disattivato via `EVENT_SOURCE_IT=serpapi` (serverless non ha browser). RA + SerpApi sono fetch → girano.
- **Android:** progetto generato ma non testato (manca toolchain/device). `npx cap run android` quando avrai Android Studio.
- **Re-firma 7gg:** limite Apple ID gratuito; per stabilità/TestFlight serve Apple Developer (99$/anno).
